# Copy to .env and fill in real values. .env is gitignored — never commit it.
# JWT secret must be >= 256 bits (32+ chars). Generate one: openssl rand -base64 48
SECURITY_JWT_SECRET=
SPRING_DATASOURCE_PASSWORD=
